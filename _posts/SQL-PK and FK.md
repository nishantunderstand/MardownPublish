FK fields exist only as numeric references
PK, FK stored as ID only, not relation
We are not forcing FK and PK on Entity.
We are storing id of 1 table or another table.

DB design is built on denormalized FK references
